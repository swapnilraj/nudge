// This file can be used to add custom configuration to app.json
// It will be merged with app.json during build

export default {
  expo: {
    // Additional configuration can be added here
    // The main config is in app.json
  },
};
