<resources xmlns:tools="http://schemas.android.com/tools">
  <style name="AppTheme" parent="Theme.AppCompat.DayNight.NoActionBar">
    <item name="android:enforceNavigationBarContrast" tools:targetApi="29">true</item>
    <item name="android:editTextBackground">@drawable/rn_edit_text_material</item>
    <item name="colorPrimary">@color/colorPrimary</item>
    <!-- Light UI: ensure status bar icons are dark and the bar isn't "invisible" on light backgrounds -->
    <item name="android:statusBarColor">#F8FAFC</item>
    <item name="android:windowLightStatusBar" tools:targetApi="23">true</item>
  </style>
  <style name="Theme.App.SplashScreen" parent="AppTheme">
    <item name="android:windowBackground">@drawable/ic_launcher_background</item>
  </style>
  <!-- No-UI activity used as a launcher trampoline -->
  <style name="Theme.App.NoDisplay" parent="@android:style/Theme.NoDisplay" />
</resources>